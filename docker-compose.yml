services:
  postgres:
    image: postgres:16-alpine
    container_name: harly-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-harly}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-harly}
      POSTGRES_DB: ${POSTGRES_DB:-harly}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - harly_postgres:/var/lib/postgresql/data

volumes:
  harly_postgres:
