# OpenHire Tooling

Developer and distribution tooling for OpenHire.

## Directories

- `create-openhire`: future `npx @openhire/create` project/bootstrap CLI.
- `docker`: Docker and deployment helpers.
- `scripts`: release, setup, and maintenance scripts.
