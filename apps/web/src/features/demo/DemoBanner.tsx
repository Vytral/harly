/**
 * @deprecated The invasive full-width demo banner was removed. Demo mode now
 * shows a one-time notice in the topbar dynamic island (see NotificationIsland).
 * This module is kept so older imports fail loudly at typecheck if revived.
 */
export function DemoBanner(): null {
  return null;
}
